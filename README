# cktop

A Linux terminal system monitor. Shows live CPU and memory usage alongside a process list with per-process CPU%, RSS, and user — reads directly from `/proc`.

## Installation

```sh
go install github.com/ckinan/cktop@latest
```

## Upgrade

```sh
GOPROXY=direct go install github.com/ckinan/cktop@latest
```

